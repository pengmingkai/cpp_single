#include <iostream>
#include <algorithm>
#define ll long long
using namespace std;

int i=0,n,k,num[40];
bool pointer[40];
ll ans=0,temp;
//涉及高精度 暂存
void explore(int x) {
    temp*=x;
    if(k==0) {
        
        for(int j=0;j<k)
    }
}

int main() {
    cin>>n>>k;
    for(;i<n;i++)
        cin>>num[i];
    
    return 0;
}
